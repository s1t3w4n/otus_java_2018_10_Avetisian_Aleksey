import com.google.gson.Gson;


/*
Домашнее задание.
        Напишите свой json object writer (object to JSON string) аналогичный gson на основе javax.json.

        Поддержите:
        - массивы объектов и примитивных типов
        - коллекции из стандартной библиотерки.
*/

/*
Добработать
    @Test
    void customCheck() {
            Assertions.assertEquals(gson.toJson(null), myJson.toJson(null));
            Assertions.assertEquals(gson.toJson(new int[] {1, 2, 3}), myJson.toJson(new int[] {1, 2, 3}));
            Assertions.assertEquals(gson.toJson(Collections.singletonList(1)), myJson.toJson(Collections.singletonList(1)));
            }*/
public class MainHW09 {

    public static void main(String[] args) {
    }
}
